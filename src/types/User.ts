export type User = {
    userName: string;
    cidNumber: string;
    fullName: string;
    email: string;
    mobileNumber: string;
    userRoles: object[];
    active: string;
    message?: string;
}